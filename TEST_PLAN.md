# 📋 웹 접근성(a11y) 자동화 테스트 기획서 (TEST_PLAN)

본 기획서는 **Playwright**와 **Axe-core**를 활용하여 프로젝트 메인 페이지의 웹 접근성 표준(WCAG 2.0 / 2.1 AA 및 국내 웹 콘텐츠 접근성 지침 KWCAG) 충족 여부를 자동 검증하기 위한 테스트 시나리오를 정의합니다.

---

## 1. 테스트 목적 및 범위
* **목적**: 장애인 및 고령자를 포함한 모든 사용자가 메인 페이지의 정보를 동등하게 탐색하고 이용할 수 있도록 보장합니다.
* **범위**: 메인 페이지(URL: `/public/`)의 홈 컴포넌트 렌더링 영역 전체
* **검사 규격**: WCAG 2.0 / 2.1 Level A 및 AA, Best Practice 규격

---

## 2. 테스트 시나리오 (Given-When-Then)

### 📌 시나리오 1: 텍스트 명도 대비 검증 (Color Contrast)
시각 장애 또는 저시력 사용자가 콘텐츠를 뚜렷하게 인식할 수 있도록 배경과 전경의 명도 대비를 검증합니다.

* **Given**: 사용자가 메인 페이지(`http://localhost:5173/public/`)에 접속해 서비스 화면이 브라우저에 정상적으로 렌더링된 상태이다.
* **When**: 접근성 자동화 도구(`@axe-core/playwright`)가 페이지 내의 모든 텍스트 요소와 배경 요소 간의 명비(Contrast Ratio)를 추출하여 분석할 때,
* **Then**: 모든 일반 텍스트는 최소 **4.5:1 이상**의 명도 대비를 유지하고, 큰 텍스트(18pt 이상 또는 bold 14pt 이상)는 최소 **3.0:1 이상**의 대비를 제공해야 한다. (명도 대비 미달로 실패하는 위반 사항이 0건이어야 한다.)

---

### 📌 시나리오 2: 구조적 랜드마크 영역 검증 (Region Landmarks)
스크린 리더 사용자가 웹 페이지의 구조를 파악하고 빠르게 탐색할 수 있도록 시맨틱 태그 구조를 검증합니다.

* **Given**: 사용자가 메인 페이지에 접속하여 돔(DOM) 구조가 완벽하게 로드된 상태이다.
* **When**: 접근성 엔진이 페이지 내 모든 핵심 정보성 영역과 컴포넌트를 스캔하여 HTML5 Landmark 태그(`main`, `nav`, `header`, `footer`, `aside`, `section` 등) 구조를 분석할 때,
* **Then**: 메인 페이지의 모든 의미 있는 콘텐츠는 적절한 시맨틱 Landmark 요소 내부에 위치해야 하며, 스크린 리더로 건너뛰기나 다이렉트 탐색이 가능하도록 구조화되어 있어야 한다. (Landmark가 지정되지 않은 정보 영역이 존재하지 않아야 한다.)

---

### 📌 시나리오 3: 키보드 내비게이션 및 포커스 가능성 검증 (Keyboard & Focusable)
마우스 사용이 불가능한 지체 장애인 및 스크린 리더 사용자가 키보드만으로 페이지 내의 모든 요소를 이용할 수 있는지 검증합니다.

* **Given**: 사용자가 키보드의 `Tab` 키만을 활용하여 메인 페이지를 내비게이션하기 시작한 상황이다.
* **When**: 접근성 스캐너가 포커스를 받을 수 있는 상호작용 가능한 요소(링크, 버튼, 폼 컨트롤 등)의 키보드 동작성을 분석할 때,
* **Then**: 
  1. 키보드로 접근 불가능하거나 숨겨진 채 포커스를 받아 조작을 방해하는 덫(Keyboard Trap)이 없어야 한다.
  2. 현재 활성화된 요소는 시각적으로 명확한 포커스 표시선(Focus Indicator)을 제공해야 한다.
  3. 논리적인 순서대로 키보드 초점(Focus Outline)이 이동해야 한다.

---

### 📌 시나리오 4: 이미지 대체 텍스트 제공 여부 검증 (Alt Text for Images)
시각 장애를 가진 사용자가 화면 낭독기를 통해 이미지의 의미를 정상적으로 이해할 수 있도록 대체 텍스트를 검증합니다.

* **Given**: 메인 페이지에 로고, 배너, 일러스트를 포함한 이미지(`<img>` 태그 및 SVG 등) 요소들이 로드되어 있는 상태이다.
* **When**: 자동화 툴이 문서 객체 모델(DOM) 내의 이미지 관련 요소들을 모두 탐색할 때,
* **Then**:
  1. 모든 의미 있는 이미지(`<img>` 태그)는 명확하고 구체적인 정보를 제공하는 `alt` 속성을 반드시 지녀야 한다.
  2. 단순 디자인/장식용 이미지는 빈 값(`alt=""`)을 제공하거나 `role="presentation"`이 정의되어 스크린 리더가 스킵할 수 있게 설정해야 한다.

---

## 3. 테스트 실행 및 리포트 생성 프로세스

자동화 기획에 따라 검사는 다음과 같이 실행됩니다.

1. **테스트 명령 실행**:
   ```bash
   npm run test:a11y
   ```
   * 백그라운드에서 Vite 개발 서버가 자동으로 실행됩니다.
   * `Playwright`가 Chromium, Firefox, WebKit을 헤드리스(Headless) 모드로 작동시켜 렌더링 결과를 시뮬레이션합니다.
   * 각 브라우저가 `/public/` 주소로 진입하여 `@axe-core/playwright`를 로드하고 위 Given-When-Then 시나리오들을 원격 평가합니다.

2. **결과 리포트 조회**:
   ```bash
   npm run test:a11y:report
   ```
   * 테스트 통과 여부 및 오류 원인의 세밀한 분석 리포트가 인터랙티브 HTML 문서로 오픈됩니다.
